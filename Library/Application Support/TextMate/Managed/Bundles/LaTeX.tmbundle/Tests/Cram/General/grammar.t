-- Setup ----------------------------------------------------------------------

  $ cd "$TESTDIR"
  $ cd ../..
  $ source lib/setup_cram.sh

-- Tests -----------------------------------------------------------------------

  $ gtm < TeX/text.tex "${grammars[@]}" > /dev/null
